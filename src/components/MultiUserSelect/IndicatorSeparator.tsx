// By default, the Select component uses a pipe character as a separator between the buttons on the right side of the multi user select menu.
// Since they're going to be replaced by Material-UI button components, this separator is no longer necessary.
// By having the component return null, nothing is rendered.
export const IndicatorSeparator = () => null;
